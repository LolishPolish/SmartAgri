# Database Configuration
# For local development, use: file:./dev.db
# For production (Turso), use: libsql://your-database.turso.io
DATABASE_URL=file:./dev.db

# Turso auth token (only needed for cloud database)
DATABASE_AUTH_TOKEN=
